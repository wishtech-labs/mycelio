# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Application
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Vercel Cron Secret (generate a strong random string)
CRON_SECRET=your_cron_secret_here

# Rate Limiting (Recommended for production - using Upstash Redis)
# Sign up at https://upstash.com/ for a free Redis database
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Additional allowed IPs for Cron jobs (comma-separated, optional)
# ALLOWED_CRON_IPS=203.0.113.0/24,198.51.100.1

# CORS Configuration for A2A endpoints (comma-separated list of allowed origins)
# Leave empty to allow same-origin only in production
# ALLOWED_A2A_ORIGINS=https://app.example.com,https://partner.example.com
